/*
 * BillList Messages
 *
 * This contains all the text for the BillList container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'BillList.containers';

export default defineMessages({});
